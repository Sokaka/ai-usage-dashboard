# AI Usage Dashboard 1.0.3

`1.0.3` 是首個正式公開版本，沿用同一組凍結候選成品，不重建、不重簽也不替換。Repository 已公開，同一筆 Release 已轉為 stable／latest；2026-09-15 完成匿名下載及簽章驗證。候選 workflow、獨立成品回讀、有界靜態隱私檢查與既有 Windows 主機離線換版均已完成；暫緩及未驗證界線列於下方。

## 本版內容

- Copilot 訂閱讀取支援 CLI `1.x` 的新舊回應：回應可包含 token、將 token 省略，或將該欄位設為 null；App 不會讀取回應中的 token。
- Copilot 通過帳號與方案核對後會顯示訂閱方案，並依 `token_based_billing` 顯示 `AI Credits` 或 `Premium requests`；計費模式缺少時顯示 `Premium usage`，訂閱讀取失敗時保留已驗證 quota 並提示問題。
- Copilot 繼續使用本機官方 CLI，要求正式版 `>=1.0.79` 且 `<2.0.0`；App 的 `GitHub.Copilot.SDK` 固定為 `1.0.11`，既有帳號、憑證與用量快取格式不變。
- 保留「關於 AI Usage」、開始選單捷徑、四種主題、五種服務卡片、精簡語系資源，以及支援與安全回報入口。

## 下載與安裝

從 [v1.0.3 GitHub Release](https://github.com/Sokaka/ai-usage-dashboard/releases/tag/v1.0.3) 取得下列同一組凍結檔案：

| 檔案 | 用途 | 大小（bytes） |
| --- | --- | ---: |
| `AiUsageDashboard-Updater-1.0.3-win-x64.exe` | 一般首次安裝與後續更新 | 35,272,246 |
| `AiUsageDashboard-Updater-1.0.3-win-x64.exe.sha256` | Updater 的 SHA256 | 108 |
| `AiUsageDashboard-1.0.3-win-x64.zip` | 完整免安裝套件，或維護人員的離線復原來源 | 77,742,353 |
| `AiUsageDashboard-1.0.3-win-x64.zip.sha256` | ZIP 的 SHA256 | 100 |
| `AiUsageDashboard-update-stable.json` | 簽署的 stable 更新清單 | 1,946 |
| `AiUsageDashboard-update-stable.json.sha256` | 更新清單的 SHA256 | 101 |

六件合計 113,016,854 bytes。不要使用 GitHub 自動產生的 Source code ZIP 作為安裝包。

正式公開沿用原私有草稿的六件檔案；2026-09-15 已從正式 v1.0.3 網址完成匿名下載、SHA256／sidecar 核對與 stable feed 簽章驗證。這項 smoke 不代表正式線上更新已驗收。安裝步驟見[使用說明](../../使用說明.md#安裝與第一次啟動)，舊內部版銜接見[升級說明](../../使用說明.md#從舊內部版升級)。

## 本版已完成的檢查

- 原始[候選 workflow](https://github.com/Sokaka/ai-usage-dashboard/actions/runs/34839329802) attempt 1 成功，Release 建置為 0 warnings／0 errors；完整測試為 4,115 passed／0 failed／0 skipped，production line coverage 75.79%（51,103／67,423）。
- Workflow 完成 source 歷史隱私檢查、更新清單簽署與驗簽，並由同一個 run 凍結六件成品。
- GitHub Release 最初凍結為 private repository 內的 draft／prerelease，tag metadata 為 `v1.0.3`，資產清單正好包含上列六個檔案；正式發布只轉正同一筆 Release。
- 已從 Release 獨立下載六件成品；asset ID、size、SHA256、三個 sidecar、build artifact 與原始 workflow freeze 全部一致。下載的 feed 另以 pinned production 公鑰完成驗簽，source、version、sequence、網址與主要成品 metadata 均相符。
- Repository 公開後，未登入的 client 已先從 latest URL 下載 stable feed 並驗簽，再下載其餘五件檔案。完整 assets pagination 已耗盡；六個 frozen asset ID、總計 113,016,854 bytes、逐件 SHA256、三份 sidecar、tag target 與受簽 payload 均相符。
- 六件成品、全部 328 個 ZIP 檔案與候選 workflow log 的有界靜態隱私檢查通過，0 findings。ZIP 完整 CRC、路徑安全、重複名稱、symlink、dependency profile、runtime pin 與五組離線 license export 亦通過；檢查未遞迴拆解單檔 EXE 內嵌內容，也未對圖片做 OCR。
- 已在既有 Windows 主機以 exact frozen Updater／ZIP 執行 `apply-local`。Updater 自然關閉 App，transaction 狀態為 `Committed`；328 個 payload、candidate archive、previous 完整舊樹、App 與 maintenance Updater 版本、Windows 登錄及開始選單均核對相符。App 已重新啟動並正常回應，未強制終止程序或產生更新警告。

先前相同 Copilot 功能的 `1.0.3-local.20260914.1` 更新後，使用者回報畫面「看起來有正常了」。Exact frozen candidate 已完成上述離線換版，但未逐欄核對方案或 AI Credits，也未執行真人 provider 驗收。

## 暫緩與未驗證項目

- 人工 UI、無障礙、首次 Windows 提示，以及五個 provider 真人登入與完整用量驗收暫緩。逐平台狀態見 [1.0.3 CLI 實測表](../CLI_COMPATIBILITY.md#103-正式版)。
- 乾淨 Windows／VM 安裝、解除安裝與復原、正式 HTTPS 更新、反降級及 Updater 自更新尚未執行。
- 本機離線換版不代替線上更新驗收；離線 installed manifest 的 source／release sequence 依設計為 null。本次成品掃描與安裝驗證未讀取帳號設定或 credentials，既有帳號與設定的保留未另做真人驗收。

服務串接依賴各平台的官方 CLI 或 SDK；上游登入流程、通訊協定或回傳格式更新時，可能暫時影響讀取。自有 EXE 沒有 Authenticode 簽章，Windows 可能提示或依政策阻擋。請保留安全防護，並閱讀 [README 使用限制](../../README.md#使用限制)。Claude `/usage` 狀態檢查可能消耗少量 token；啟用 usage credits 時也可能產生額外費用。Claude／Antigravity 的整合條款適用性仍未確認；既有[發布限制](../../RELEASING.md#最後公開決策)持續適用。

## 成品來源

- Source：[0b7a5d922e56b9590370d776a99642d5dd671b2c](https://github.com/Sokaka/ai-usage-dashboard/commit/0b7a5d922e56b9590370d776a99642d5dd671b2c)。
- Release sequence：`1017`，channel：`stable`。
- App ZIP SHA256：`f5d69ec4cf008accbc794c34be0dc66fe4ce5c163fc1ce72e121deee227a2884`。
- Updater SHA256：`dfcd5db6f06729d78739b3b87f91fe84de72bf44119a9dde2ab7e0e6be2ec129`。
- Feed SHA256：`d4dc8c42f8140a50f8d3ab196d485b98c2aab470e5a34618368384812c0520f7`。

本說明在候選凍結後整理，不修改或重新封裝候選成品。詳細驗證界線見[實作與驗證清單](../../IMPLEMENTATION_CHECKLIST.md#目前正式版本)。
