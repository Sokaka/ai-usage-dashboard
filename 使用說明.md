# AI Usage 使用說明

## 簡介

AI Usage 是 Windows 桌面工具，可在同一個浮窗查看 Claude、Codex、
GitHub Copilot、Grok 與 Antigravity 的訂閱用量。

- 帳號設定與上次用量預設只保存在目前 Windows 使用者的電腦中。
- 這是 Sokaka 的個人作品；請使用自己的服務帳號，並遵守適用條款。

### 先確認你使用的版本

- **版本**：查看下載檔名或 Windows **已安裝的應用程式**。
- **指南**：優先閱讀安裝包隨附版本；GitHub 文件可能較新。
- **下載與支援**：見[目前版本][project-readme]及該版本的 Release 說明。

## 快速導覽

- 第一次使用：[安裝與第一次啟動](#安裝與第一次啟動)
- 加入服務：[連接帳號](#連接帳號)
- 調整浮窗：[查看與管理用量](#查看與管理用量)
- 搬移設定：[匯入與匯出設定](#匯入與匯出設定)
- 更新、換機或移除：[更新、換機與移除](#更新換機與移除)
- 遇到問題：[常見問題](#常見問題)

## 環境需求

- 仍在 Microsoft 支援範圍內的 64 位元 Windows 電腦（x64）。
  不支援 32 位元或 ARM 版本。
- 可連上網路。連接帳號、更新用量與線上更新都需要網路。
- 不必另外安裝 .NET，安裝包已包含所需元件。

### 支援平台

只需準備要使用的平台：

| 平台 | 使用前準備 | 帳號卡片 |
| --- | --- | --- |
| Claude | Claude Code 2.1.169 以上版本，以及 Claude.ai Pro／Max／Team 帳號。 | 多帳號；同帳號依不同組織分卡 |
| Codex | 官方 Codex CLI 與 ChatGPT 帳號。 | 多帳號；同帳號依不同工作區分卡 |
| GitHub Copilot | 可使用 Copilot 的 `github.com` 帳號；安裝包已附上所需工具。 | 多帳號；同帳號限一張卡片 |
| Grok | 從 xAI 官方來源安裝到預設位置的 Grok Build CLI。 | 多帳號；同帳號限一張卡片 |
| Antigravity | Windows 10 版本 1809 以上，以及 1.1.11 以上、2.0.0 未滿的正式版。 | 限一個帳號 |

- **官方來源**：AI Usage 會檢查工具來源與相容性。若不符，請依提示更新或聯絡提供安裝包的人。
- **舊 Antigravity 連接**：已連接的 1.1.7／1.1.9 通過檢查後可繼續使用；新增或重新連接前，須更新到表列範圍。

## 畫面與登入用語

| 用語 | 意思 |
| --- | --- |
| 卡片 | 浮窗中的一個服務帳號及其用量。 |
| 存取權杖（token） | 代表登入授權的一段文字，請像密碼一樣保管。 |
| 回傳網址（callback URL） | 登入網頁交回工具、用來繼續登入的網址。 |
| 工作區 ID（workspace ID） | Codex 用來分辨工作區的代碼。 |

## 使用說明

### 安裝與第一次啟動

如果正在使用舊的免安裝版，請先從系統匣選擇 **結束 AI Usage**，
確認視窗與背景程式都已關閉。

#### 使用正式更新程式

請使用專案提供的更新程式：

1. 從正式 GitHub Releases 下載並保留
   `AiUsageDashboard-Updater-<version>-win-x64.exe`。
2. 直接執行，不要使用系統管理員身分。
3. 首次使用請閱讀適用條款，選擇接受或退出。更新程式會驗證更新來源與檔案完整性，再安裝並啟動 AI Usage。
4. 選擇 **新增帳號**，完成連接。

正常安裝後，AI Usage 會出現在 Windows
**設定 → 應用程式 → 已安裝的應用程式**。若沒有出現在清單中，但程式仍可開啟，
請保留警告內容並聯絡提供安裝包的人。

請從[專案指定的 Releases](https://github.com/Sokaka/ai-usage-dashboard/releases)下載。更新程式只使用公開下載來源，請勿把存取權杖（token）加入下載網址。

#### 使用離線 ZIP

1. 下載 `AiUsageDashboard-<version>-win-x64.zip`。
   不要下載 GitHub 自動產生的 Source code ZIP。
2. ZIP 若經過轉傳，請先依[進階：核對 ZIP 的 SHA-256](#核對-zip-的-sha-256)
   確認檔案。
3. 將 ZIP 完整解壓到新的資料夾。不要直接在 ZIP 內執行，也不要只複製單一 EXE。
4. 開啟解壓後的 `AiUsageDashboard` 資料夾，再執行
   `app\AiUsageDashboard.App.exe`。

#### 再次開啟與自動啟動

AI Usage 主要使用系統匣，不會顯示一般的工作列視窗按鈕。

- 浮窗隱藏時，從系統匣選擇 **顯示浮窗**，或雙擊 AI Usage 圖示。
- 更新程式安裝的版本若已完全結束，按 `Win + R` 後執行：

```text
%LOCALAPPDATA%\Programs\AiUsageDashboard\current\app\AiUsageDashboard.App.exe
```

- 離線 ZIP 版請從自己保留的資料夾執行 `app\AiUsageDashboard.App.exe`。
- 更新程式安裝的版本可從系統匣選擇 **Windows 登入啟動項：未登錄**。
  再次選擇即可移除。若 Windows 另行停用該啟動項，請從同一選單開啟設定確認。
- 離線 ZIP 與自訂安裝位置不能使用登入自動啟動功能。

### 條款與離線授權文件

- **同意條款**：安裝或首次啟動時閱讀，選擇接受或退出；條款未變時不必重複確認，更新後須重新接受。
- **同意紀錄**：只存於目前 Windows 使用者的電腦，不會上傳。
- **離線閱讀／匯出**：使用 `--licenses` 或 `--export-licenses <新目錄>`。自動化安裝命令見[維護手冊][internal-distribution]。
- **移除與復原**：解除安裝、為更新關閉程式及人工離線復原，不受一般啟動的同意提示阻擋。

### 舊 internal 安裝銜接

1. 下載正式版更新程式。
2. 使用原 Windows 使用者及一般權限，在原安裝位置手動更新一次。
3. 確認程式版本及 Windows **已安裝的應用程式**；有警告時保留訊息並聯絡提供者。

既有帳號與設定會保留，請勿另行複製登入資料。自訂安裝位置或排錯請見[維護手冊][internal-distribution]。

### 連接帳號

#### 連接 Claude 前必讀

- **費用**：查詢會送出少量內容，可能增加用量或費用。AI Usage 只能事後檢查，已產生的用量或費用無法撤回。
- **同意**：新增帳號時會先說明風險；按 **儲存並連接** 即表示接受。
- **再次查詢**：若上次是否完成不明，卡片會暫停。確認可以再執行一次後，才按 **重新檢查 Claude 用量**。

#### 連接步驟

選擇 **新增帳號** 與服務類型，再按 **儲存並連接**：

| 平台 | 登入操作 |
| --- | --- |
| Claude | 完成 Claude 官方登入；若瀏覽器顯示驗證碼，貼回登入視窗。 |
| Codex | 在瀏覽器完成 ChatGPT 登入。 |
| GitHub Copilot | 在 GitHub 官方頁面登入，確認最後顯示的是要加入的帳號。 |
| Grok | 在 AI Usage 開啟的瀏覽器登入；若要求 token、授權碼或 callback URL，貼回剛開啟的終端並按 Enter。 |
| Antigravity | 先登入 Antigravity CLI，依畫面確認帳號與用量，再按 **完成連接**。 |

若要稍後再連接，可先按 **儲存**，之後從卡片的 **帳號設定** 繼續。

- **Codex 多工作區**：加入第二個工作區時，第一張卡片也須重新連接並選擇原工作區。每張卡片須有不同的工作區 ID，才能正確區分。
- **Grok 帳號辨識**：不一定提供電子郵件，可用暱稱區分卡片。
- **Copilot 登入**：各卡片分開保存登入資料。出現 `401` 或登入失效時須重新連接；AI Usage 不會在背景自行登入。

### 查看與管理用量

- 浮窗會自動更新，也可按 **檢查用量** 手動更新。
- 卡片顯示 **顯示上次資料** 時，請留意資料時間。暫時失敗不代表需要重新登入。
- 可切換 **已使用** 與 **剩餘**，並選擇手動或自動排序。
  自動排序依服務、主要用量週期與重置時間排列，不看用量百分比；
  切回手動排序後會恢復原本順序。
- 浮窗可置頂、收合、拖曳與停靠到畫面角落。
- 從 **⋯** 勾選 **高度隨卡片數量調整**，浮窗會依內容調整高度。
- 從 **主題配色** 可切換經典藍、曜石黑、柔霧灰與櫻花粉。
- 卡片以平台名稱的專屬色辨識服務；黃色表示用量警告，紅色表示額度用盡。Windows 高對比模式會使用系統配色。
- 隱藏浮窗後仍會在系統匣更新。要完全結束，請選擇 **結束 AI Usage**。

從卡片的 **⋯** 選單可修改設定、暫停檢查、調整順序或移除帳號。

- **移除卡片**不代表已從服務登出。
- **Copilot**：清除該卡片在 Windows 認證管理員的登入資料，不影響其他卡片。
- **Grok**：清除該卡片另存的登入資料，不影響 Grok Build CLI 的預設登入狀態。

#### GitHub Copilot 的用量欄位

依帳號計費方式顯示 **AI Credits** 或 **Premium requests**；無法確認時顯示 **Premium usage**。

- **個人方案**：有回傳額度時，顯示已使用量與上限。
- **Business／Enterprise**：組織共用額度與預算；未顯示完整上限不代表無上限，請以 GitHub Copilot 或組織的 Billing 設定為準。
- **細項**：Copilot Chat 已含在 AI Credits 中；Code completions 分開顯示。

### 匯入與匯出設定

從浮窗的 **⋯** 選單開啟 **匯入或匯出設定**。

| 匯出內容 | 範圍 |
| --- | --- |
| 包含 | 帳號卡、暱稱、排序、用量顯示、主題與浮窗偏好。 |
| 不包含 | 密碼、token、服務登入資料、上次用量、Antigravity 電腦設定及 Windows 登入啟動設定。 |
| 隱私 | 未加密，可能含電子郵件等帳號資訊；請勿公開。 |

- 匯入前先預覽，確認後才取代目前設定；失敗時自動還原。
- 匯入後須依提示重新連接帳號。換機時如需 Windows 登入啟動，須另行登錄。

#### 匯入後還原

成功匯入後，可在本次開啟程式期間選擇 **還原匯入前設定**。
需要還原時請立刻操作；下列任一操作都會關閉還原功能：

- 重新啟動程式。
- 新增、修改、啟用、停用、移除或重新排列卡片。
- 切換已使用／剩餘用量，或切換手動／自動排序。
- 展開／收合浮窗、切換置頂、改變停靠角落、主題或自動調整高度。

單純顯示或隱藏浮窗不會關閉還原功能。若匯入後的浮窗是收合狀態，
請直接從系統匣選擇 **還原匯入前設定**，不要先展開浮窗。

### 更新、換機與移除

#### 更新

- 執行原先保留的更新程式，即可檢查並安裝更新。
- 離線 ZIP 請解壓到新資料夾，不要覆蓋舊版。
- 帳號與顯示設定不在版本資料夾內，正常更新時不必搬移。
- 更新失敗時，請停止操作並聯絡提供安裝包的人。

若更新後 Claude 卡片顯示 **需要確認 Claude 訂閱**，請按 **確認 Claude 訂閱**，
使用這張卡片原本的帳號完成官方登入，再確認組織與訂閱方案。
帳號資料仍保留；若出現舊用量確認視窗，請依資料的實際歸屬選擇保留、清除或取消。
若卡片提示切換帳號，請先使用原本的帳號確認連接，不必改用另一個帳號。

#### 換機

1. 在舊電腦選擇 **匯出設定**。
2. 在新電腦安裝 AI Usage。
3. 選擇 **匯入設定**。
4. 依卡片提示重新連接帳號。
5. 如需登入後自動啟動，請在新電腦重新登錄。

不要直接複製舊電腦或其他 Windows 使用者的 AI Usage 資料夾。

#### 移除

- **由更新程式安裝**：從 Windows
  **設定 → 應用程式 → 已安裝的應用程式**解除安裝。
  帳號設定與使用偏好會保留。
- **離線 ZIP**：完全結束 AI Usage，再刪除解壓後的版本資料夾。
- **清除 AI Usage 的帳號與設定**：先在 AI Usage 逐張移除 Copilot 卡片並確認完成，
  再完全結束程式、解除安裝或刪除離線版，最後刪除下列資料夾。
  若曾連接 Antigravity，這會一併刪除它的本機連接與私密校準資料，下次使用時必須
  重新連接。若要保留既有連接，不要刪除此資料夾，請先聯絡支援人員。

```text
%LOCALAPPDATA%\AiUsageDashboard
```

只刪除這個資料夾，不會清除 Windows 認證管理員內的 Copilot 登入資料。
若 AI Usage 已無法開啟，請勿自行刪除不確定的認證項目，改由支援人員處理。
這個流程仍會保留 AI Usage 為 Claude、Codex、Grok 保存的執行檔副本。
若要連這些工具檔案一起永久清除，請聯絡支援人員。
上述操作不會刪除各服務上的帳號。

## 注意事項

- Claude、Codex、GitHub Copilot、Grok 與 Antigravity 串接目前都是實驗性功能。
  服務變更登入或用量格式時，可能暫時無法讀取。
- 安裝包若沒有版本號，也無法確認來源，請勿執行。
- Windows 執行檔尚無 Authenticode 簽章，可能出現安全提示或依系統政策阻擋。不要關閉 SmartScreen 或其他安全防護。
- SHA-256 的預期值必須來自另一個可信任管道，不能和 ZIP 一起轉傳。
- Claude 用量查詢可能增加少量用量或費用，且無法撤回。
- 從 AI Usage 移除卡片不等於從服務登出。
- 匯出檔未加密，可能包含帳號資訊。請勿放在公開位置。
- 不要把個人檔案放進更新程式管理的安裝目錄。

## 常見問題

- **浮窗不見了**：從系統匣選擇 **顯示浮窗**，或雙擊 AI Usage 圖示。
- **完全結束後不知道怎麼開啟**：更新程式安裝版請使用上方固定路徑；
  離線 ZIP 請執行自己保留資料夾內的 `app\AiUsageDashboard.App.exe`。
- **登入後沒有自動啟動**：確認 Windows 的 **啟動應用程式**沒有停用 AI Usage。
- **更新程式無法下載**：確認使用專案提供的更新程式及公開的 Release。更新程式不會沿用瀏覽器的登入權限；簽章、版本或來源驗證失敗時，先停止並保留錯誤訊息。
- **沒有出現在已安裝的應用程式**：程式仍可能已安裝。保留警告並聯絡提供者。
- **顯示上次資料或讀取失敗**：確認網路並等待自動重試，不必立刻重新登入。
- **顯示需要重新連接**：使用卡片上的連接或切換帳號按鈕。
- **Copilot 顯示 `401`**：重新連接該張卡片。
- **官方工具缺少、來源不符或版本不支援**：從官方來源安裝或更新，
  再按 **完成後再檢查**。
- **Grok 要求存取權杖或回傳網址**：貼回 AI Usage 開啟的登入終端。
- **Antigravity 顯示用量檢查已暫停**：確認可以再執行一次後，
  按 **重新檢查 Antigravity 用量**。
- **Antigravity 暫時無法讀取**：AI Usage 會保留上次的帳號與用量，
  並在背景自動再試；重新啟動程式後也會繼續。
- **Antigravity 更新後仍無法讀取**：既有連接通常可沿用。
  若卡片要求操作，請重新確認連接；若顯示用量格式不支援，請更新 AI Usage。
- **問題持續**：提供 AI Usage 版本、安裝檔名、Windows 版本、官方工具版本
  與完整錯誤。截圖前遮住帳號資訊；不要傳送密碼、token 或登入資料。

## 資料與隱私

AI Usage 的帳號卡片、顯示偏好與上次用量主要儲存在：

```text
%LOCALAPPDATA%\AiUsageDashboard
```

AI Usage 透過各服務的官方工具或 SDK 完成登入與用量查詢，
不會讀取密碼或瀏覽器登入資料。

Copilot 每張卡片的 token 會分開保存在 Windows 認證管理員，
不會寫入帳號設定、上次用量、匯出檔或診斷紀錄。
Grok 也會依卡片分開保存本機登入資料。

使用 Antigravity 時，AI Usage 會保存這台電腦所需的連接設定。
卡片顯示的電子郵件與方案只存於本機，不會寫入匯出檔，也不會當成登入憑證。

安裝包已包含 Copilot 所需元件，並附上
[Copilot CLI](third-party-notices/GitHub-Copilot-CLI-LICENSE.md)與
[Copilot SDK](third-party-notices/GitHub-Copilot-SDK-LICENSE.md)的第三方授權聲明。

## 進階資料

### 核對 ZIP 的 SHA-256

ZIP 經過聊天軟體、網路磁碟或他人轉傳時，請從另一個可信任管道取得同版本的 SHA-256，
例如已核對的正式 Release 發布資訊。不要只使用和 ZIP 一起轉傳的雜湊值。

在 ZIP 所在的資料夾開啟 PowerShell，執行下列命令，並把檔名換成實際版本：

```powershell
Get-FileHash -LiteralPath ".\AiUsageDashboard-<version>-win-x64.zip" -Algorithm SHA256
```

畫面中的 `Hash` 必須與可信任管道提供的值相同。同一個下載位置提供的 ZIP 與
`.sha256` 可以檢查下載是否損壞，但不能單獨證明檔案由誰發布。

### 維護文件

以下文件供安裝支援與維護人員使用，內容以 `main` 為準，可能與安裝版本不同：

- [技術總覽][technical-overview]：程式架構、資料保存與服務串接。
- [Windows 分發與支援手冊][internal-distribution]：安裝、更新、回復與清理。
- [發佈流程][releasing]：建立與驗證候選版。

[project-readme]: https://github.com/Sokaka/ai-usage-dashboard/blob/main/README.md#目前版本
[technical-overview]: https://github.com/Sokaka/ai-usage-dashboard/blob/main/docs/TECHNICAL_OVERVIEW.md
[internal-distribution]: https://github.com/Sokaka/ai-usage-dashboard/blob/main/INTERNAL_DISTRIBUTION.md
[releasing]: https://github.com/Sokaka/ai-usage-dashboard/blob/main/RELEASING.md
