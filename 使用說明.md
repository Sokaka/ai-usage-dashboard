# AI Usage 使用說明

## 簡介

AI Usage 是 Windows 桌面工具，可在同一個浮窗查看 Claude、Codex、
GitHub Copilot、Grok 與 Antigravity 的訂閱用量。

Claude、Codex、GitHub Copilot 與 Grok 可加入多個帳號；Antigravity 目前限一個帳號。
帳號設定與上次取得的用量預設只保存在目前 Windows 使用者的電腦中。

這是 Sokaka 的個人作品。公開 stable 版本的準備與驗收狀態請見專案 README；各服務仍須使用你自己的帳號並遵守適用條款。

### 先確認你使用的版本

這份說明可能出現在 GitHub、離線 ZIP 根目錄或已安裝程式的 `app` 資料夾中：

- 從安裝包或已安裝程式開啟時，這份說明只描述該安裝包。
  可從下載檔名或 Windows **已安裝的應用程式**確認版本。
- 從 GitHub 的 `main` 分支或本機原始碼開啟時，內容可能比已安裝版本新，
  不能直接套用到已安裝版本；請以該安裝包附的說明為準。

需要選擇下載版本時，請查看[專案 README 的「目前版本」][project-readme]。
請以下載版本的 Release 說明及隨附指南為準。

## 快速導覽

- 第一次使用：[安裝與第一次啟動](#安裝與第一次啟動)
- 加入服務：[連接帳號](#連接帳號)
- 調整浮窗：[查看與管理用量](#查看與管理用量)
- 搬移設定：[匯入與匯出設定](#匯入與匯出設定)
- 更新、換機或移除：[更新、換機與移除](#更新換機與移除)
- 遇到問題：[常見問題](#常見問題)

## 環境需求

- 仍在 Microsoft 支援範圍內的 64 位元 Windows 電腦（x64）。
  不支援 32 位元或 ARM 版本。
- 可連上網路。連接帳號、更新用量與線上更新都需要網路。
- 不必另外安裝 .NET，安裝包已包含所需元件。

使用哪一項服務，就要先準備該服務的帳號與官方工具：

| 服務 | 使用前準備 |
| --- | --- |
| Claude | Claude Code 2.1.169 以上版本，以及 Claude.ai Pro／Max／Team 帳號。 |
| Codex | Codex CLI 與 ChatGPT 帳號；目前以 0.144.1 測試。 |
| GitHub Copilot | 可使用 Copilot 的 `github.com` 帳號；安裝包已附上所需工具。 |
| Grok | 從 xAI 官方來源安裝到預設位置的 Grok Build CLI；目前以 1.0.3 測試。 |
| Antigravity | Windows 10 版本 1809 以上，以及 1.1.11 以上、2.0.0 未滿的正式版。 |

表中的 Codex 與 Grok 測試版本只供排錯參考，不代表其他官方版本一定不能使用。
AI Usage 仍會檢查工具來源、版本格式與必要功能。

舊版 AI Usage 已連接的 Antigravity 1.1.7 或 1.1.9，在通過程式檢查後仍可繼續使用。
新增或重新連接 Antigravity 帳號前，請先把 Antigravity 更新到表列的支援範圍。

請從各服務的官方來源安裝工具。若畫面顯示來源或版本不符，
請依提示更新，或聯絡提供安裝包的人。

## 畫面與登入用語

- **卡片**：浮窗中顯示一個服務帳號及其用量的區塊。
- **存取權杖（token）**：代表登入授權的一段文字，請像密碼一樣保管。
- **回傳網址（callback URL）**：登入網頁完成後，交回工具繼續登入的網址。
- **工作區 ID（workspace ID）**：Codex 用來分辨工作區的代碼。

## 使用說明

### 安裝與第一次啟動

如果正在使用舊的免安裝版，請先從系統匣選擇 **結束 AI Usage**，
確認視窗與背景程式都已關閉。

#### 使用正式更新程式

正式 stable Release 公開且標示驗收通過後，依下列步驟操作：

1. 從正式 GitHub Releases 下載並保留
   `AiUsageDashboard-Updater-<version>-win-x64.exe`。
2. 直接執行，不要使用系統管理員身分。
3. 首次正常使用前閱讀適用條款，選擇接受或退出；更新程式先驗證 signed feed，再下載核對最新版，安裝後啟動 AI Usage。
4. 選擇 **新增帳號**，完成連接。

正常安裝後，AI Usage 會出現在 Windows
**設定 → 應用程式 → 已安裝的應用程式**。若沒有出現在清單中，但程式仍可開啟，
請保留警告內容並聯絡提供安裝包的人。

private 候選 Release 無法使用匿名正式下載。瀏覽器或 GitHub CLI（`gh`）登入不會提供更新程式權限；不要把 token 加進 URL。正式下載來源為 `https://github.com/Sokaka/ai-usage-dashboard/releases`。

#### 使用離線 ZIP

1. 下載 `AiUsageDashboard-<version>-win-x64.zip`。
   不要下載 GitHub 自動產生的 Source code ZIP。
2. ZIP 若經過轉傳，請先依[進階：核對 ZIP 的 SHA-256](#核對-zip-的-sha-256)
   確認檔案。
3. 將 ZIP 完整解壓到新的資料夾。不要直接在 ZIP 內執行，也不要只複製單一 EXE。
4. 開啟解壓後的 `AiUsageDashboard` 資料夾，再執行
   `app\AiUsageDashboard.App.exe`。

#### 再次開啟與自動啟動

AI Usage 主要使用系統匣，不會顯示一般的工作列視窗按鈕。

- 浮窗隱藏時，從系統匣選擇 **顯示浮窗**，或雙擊 AI Usage 圖示。
- 更新程式安裝的版本若已完全結束，按 `Win + R` 後執行：

```text
%LOCALAPPDATA%\Programs\AiUsageDashboard\current\app\AiUsageDashboard.App.exe
```

- 離線 ZIP 版請從自己保留的資料夾執行 `app\AiUsageDashboard.App.exe`。
- 更新程式安裝的版本可從系統匣選擇 **Windows 登入啟動項：未登錄**。
  再次選擇即可移除。若 Windows 另行停用該啟動項，請從同一選單開啟設定確認。
- 離線 ZIP 與自訂安裝位置不能使用登入自動啟動功能。

### 條款與離線授權文件

首次啟動 portable App、Setup 或 Updater 時可先閱讀適用條款，再接受或退出。紀錄僅存在同一 Windows 使用者的電腦，依條款內容版本與涵蓋範圍共用，不綁定 provider 帳號、不上傳。有效接受已涵蓋時，背景查詢與 helper 回呼不重複提示；條款變更後必須重新確認。

App、Setup、Updater 與 capture helper 提供 `--licenses` 閱讀、`--export-licenses <新目錄>` 離線匯出，以及 `--accept-licenses <本版顯示的 digest>` 明確預先接受。非互動入口缺少有效接受會停止；請先閱讀同一版本的文字。授權提示不會混入 helper 的回呼輸出。解除安裝、關閉 App 供更新與人工離線復原不受一般啟動提示阻擋。

### 舊 internal 安裝銜接

舊 internal Updater 的 channel 已固定；只換 `--feed-url` 不會切到 stable。正式版本驗收通過後，從正式 Release 取得新 stable Updater，以原 Windows 使用者、一般權限，在原 install root 手動執行一次；自訂安裝位置以 `--install-root` 明確指定原目錄。

保留 `%LOCALAPPDATA%\AiUsageDashboard`、既有帳號、Credential Manager 與受保護 CLI，不複製或重建登入資料。完成後核對版本、maintenance 副本及 Windows 已安裝的應用程式；有警告就不要當成全部成功。後續更新使用 stable feed，仍會拒絕降版與同版本異內容。此銜接的實機結果以該 Release 為準。

### 連接帳號

#### 連接 Claude 前必讀

Claude 的用量查詢會送出少量內容，因此可能增加用量或費用。
AI Usage 只能在查詢完成後檢查結果；已產生的用量或費用無法撤回。

新增 Claude 帳號時，畫面會先說明風險。按 **儲存並連接** 即表示接受。
若程式無法確認上次檢查是否完成，會暫停該卡片並顯示
**重新檢查 Claude 用量**。確認可以再執行一次後才按下按鈕。

#### 連接步驟

選擇 **新增帳號** 與服務類型，再按 **儲存並連接**：

- **Claude**：依畫面完成 Claude 官方登入。若瀏覽器顯示驗證碼，請貼回登入視窗。
- **Codex**：依瀏覽器畫面完成 ChatGPT 登入。
- **GitHub Copilot**：在 GitHub 官方頁面完成登入，並確認最後顯示的是要加入的帳號。
- **Grok**：AI Usage 會開啟登入終端與瀏覽器。若網頁要求存取權杖（token）、
  授權碼或回傳網址（callback URL），請貼回剛開啟的終端並按 Enter。
- **Antigravity**：先登入 Antigravity CLI，再依畫面確認帳號與用量，
  最後按 **完成連接**。

若要稍後再連接，可先按 **儲存**，之後從卡片的 **帳號設定** 繼續。

| 服務 | 同一登入帳號可否加入多張卡片 |
| --- | --- |
| Claude | 可以，但每張卡片必須選擇不同組織。 |
| Codex | 可以，但每張卡片必須選擇不同工作區（workspace）。 |
| GitHub Copilot | 不可以；同一個 `github.com` 帳號只能加入一次。 |
| Grok | 不可以；同一個 Grok 帳號只能加入一次。 |
| Antigravity | 目前只能加入一個帳號。 |

同一個 Codex 帳號加入第二個工作區時，原本第一張卡片也要重新連接，
並選擇它自己的工作區。最後每張卡片都必須使用不同的工作區 ID，
否則 AI Usage 無法正確區分。

Grok 不一定提供電子郵件，可用暱稱區分卡片。
Copilot 連接完成後，每張卡片的登入資料會分開保存。
若顯示 `401` 或登入失效，請重新連接；AI Usage 不會在背景自行登入。

### 查看與管理用量

- 浮窗會自動更新，也可按 **檢查用量** 手動更新。
- 卡片顯示 **顯示上次資料** 時，請留意資料時間。暫時失敗不代表需要重新登入。
- 可切換 **已使用** 與 **剩餘**，並選擇手動或自動排序。
  自動排序依服務、主要用量週期與重置時間排列，不看用量百分比；
  切回手動排序後會恢復原本順序。
- 浮窗可置頂、收合、拖曳與停靠到畫面角落。
- 從 **⋯** 勾選 **高度隨卡片數量調整**，浮窗會依內容調整高度。
- 從 **主題配色** 可切換經典藍、曜石黑、柔霧灰與櫻花粉。
- 卡片以平台名稱的專屬色辨識服務，方案與暱稱維持一般文字色。
  名稱與正常用量條採同一色系，亮度會配合主題調整；用量警告以黃色系顯示，
  額度用盡則以紅色系顯示。Windows 高對比模式會改用系統配色。
- 隱藏浮窗後仍會在系統匣更新。要完全結束，請選擇 **結束 AI Usage**。

從卡片的 **⋯** 選單可修改設定、暫停檢查、調整順序或移除帳號。
移除卡片不代表已從服務登出。移除 Copilot 卡片時，AI Usage 會清除
這張卡片保存在 Windows 認證管理員的登入資料，不會影響其他卡片。
移除 Grok 卡片時，AI Usage 會清除為該卡片另存的登入資料，
但不會影響 Grok Build CLI 預設的登入狀態。

#### GitHub Copilot 的用量欄位

- 新制顯示 **AI Credits**。
- 舊制顯示 **Premium requests**。
- 無法確認計費方式時顯示 **Premium usage**。

個人方案若有回傳額度，卡片會顯示已使用量與上限。
Business／Enterprise 方案由組織共用額度與預算；卡片未顯示完整上限，
不代表額度無上限。實際限制請以 GitHub Copilot 或組織的 Billing 設定為準。
Copilot Chat 已包含在 AI Credits 中；Code completions 會分開顯示。

### 匯入與匯出設定

從浮窗的 **⋯** 選單開啟 **匯入或匯出設定**。

- 匯出檔包含帳號卡、暱稱、排序、用量顯示、主題與浮窗偏好。
- 匯出檔不包含密碼、存取權杖、服務登入資料、上次用量或 Antigravity 電腦設定。
- 匯出檔未加密，可能包含電子郵件等帳號資訊。
- 匯入前會顯示預覽，確認後才取代目前設定。
- 匯入失敗時會自動還原。匯入成功後，在本次開啟程式期間仍可選擇
  **還原匯入前設定**。需要還原時請立刻操作；重新啟動程式，或執行下列任一操作後，
  還原功能就會關閉：
  - 新增、修改、啟用、停用、移除或重新排列卡片。
  - 切換已使用／剩餘用量，或切換手動／自動排序。
  - 展開／收合浮窗、切換置頂、改變停靠角落、主題或自動調整高度。
  單純顯示或隱藏浮窗不會關閉還原功能。若匯入後的浮窗是收合狀態，
  請直接從系統匣選擇 **還原匯入前設定**，不要先展開浮窗。
- 匯入不會保留登入狀態。換機或匯入後，請依卡片提示重新連接。
- Windows 登入啟動設定不會隨匯出檔移轉。

### 更新、換機與移除

#### 更新

- 線上更新啟用後，執行原本保留的更新程式即可。
- GitHub 私有預發行版仍須由有權限的維護人員下載並安裝。
- 離線 ZIP 請解壓到新資料夾，不要覆蓋舊版。
- 帳號與顯示設定不在版本資料夾內，正常更新時不必搬移。
- 更新失敗時，請停止操作並聯絡提供安裝包的人。

若更新後 Claude 卡片顯示 **需要確認 Claude 訂閱**，請按 **確認 Claude 訂閱**，
使用這張卡片原本的帳號完成官方登入，再確認組織與訂閱方案。
帳號資料仍保留；若出現舊用量確認視窗，請依資料的實際歸屬選擇保留、清除或取消。
舊版若在更新後改為提示切換帳號，也請先使用原本的帳號確認連接，不必改用另一個帳號。

#### 換機

1. 在舊電腦選擇 **匯出設定**。
2. 在新電腦安裝 AI Usage。
3. 選擇 **匯入設定**。
4. 依卡片提示重新連接帳號。
5. 如需登入後自動啟動，請在新電腦重新登錄。

不要直接複製舊電腦或其他 Windows 使用者的 AI Usage 資料夾。

#### 移除

- **由更新程式安裝**：從 Windows
  **設定 → 應用程式 → 已安裝的應用程式**解除安裝。
  帳號設定與使用偏好會保留。
- **離線 ZIP**：完全結束 AI Usage，再刪除解壓後的版本資料夾。
- **清除 AI Usage 的帳號與設定**：先在 AI Usage 逐張移除 Copilot 卡片並確認完成，
  再完全結束程式、解除安裝或刪除離線版，最後刪除下列資料夾。
  若曾連接 Antigravity，這會一併刪除它的本機連接與私密校準資料，下次使用時必須
  重新連接。若要保留既有連接，不要刪除此資料夾，請先聯絡支援人員。

```text
%LOCALAPPDATA%\AiUsageDashboard
```

只刪除這個資料夾，不會清除 Windows 認證管理員內的 Copilot 登入資料。
若 AI Usage 已無法開啟，請勿自行刪除不確定的認證項目，改由支援人員處理。
這個流程仍會保留 AI Usage 為 Claude、Codex、Grok 保存的執行檔副本。
若要連這些工具檔案一起永久清除，請聯絡支援人員。
上述操作不會刪除各服務上的帳號。

## 注意事項

- Claude、Codex、GitHub Copilot、Grok 與 Antigravity 串接目前都是實驗性功能。
  服務變更登入或用量格式時，可能暫時無法讀取。
- 使用私有預發行版前，先確認該版本是否包含你要用的功能。
- 安裝包若沒有版本號，也無法確認來源，請勿執行。
- Windows 執行檔尚未簽章。不要關閉 SmartScreen 或其他安全防護。
- SHA-256 的預期值必須來自另一個可信任管道，不能和 ZIP 一起轉傳。
- Claude 用量查詢可能增加少量用量或費用，且無法撤回。
- 從 AI Usage 移除卡片不等於從服務登出。
- 匯出檔未加密，可能包含帳號資訊。請勿放在公開位置。
- 不要把個人檔案放進更新程式管理的安裝目錄。

## 常見問題

- **浮窗不見了**：從系統匣選擇 **顯示浮窗**，或雙擊 AI Usage 圖示。
- **完全結束後不知道怎麼開啟**：更新程式安裝版請使用上方固定路徑；
  離線 ZIP 請執行自己保留資料夾內的 `app\AiUsageDashboard.App.exe`。
- **登入後沒有自動啟動**：確認 Windows 的 **啟動應用程式**沒有停用 AI Usage。
- **更新程式無法下載**：確認使用正式 stable Updater 及已公開的 Release。簽章、版本或來源驗證失敗時先停止並保留錯誤訊息。
- **沒有出現在已安裝的應用程式**：程式仍可能已安裝。保留警告並聯絡提供者。
- **顯示上次資料或讀取失敗**：確認網路並等待自動重試，不必立刻重新登入。
- **顯示需要重新連接**：使用卡片上的連接或切換帳號按鈕。
- **Copilot 顯示 `401`**：重新連接該張卡片。
- **官方工具缺少、來源不符或版本不支援**：從官方來源安裝或更新，
  再按 **完成後再檢查**。
- **Grok 要求存取權杖或回傳網址**：貼回 AI Usage 開啟的登入終端。
- **Antigravity 顯示用量檢查已暫停**：確認可以再執行一次後，
  按 **重新檢查 Antigravity 用量**。
- **Antigravity 暫時無法讀取**：AI Usage 會保留上次的帳號與用量，
  並在背景自動再試；重新啟動程式後也會繼續。
- **Antigravity 更新後仍無法讀取**：既有連接通常可沿用。
  若卡片要求操作，請重新確認連接；若顯示用量格式不支援，請更新 AI Usage。
- **問題持續**：提供 AI Usage 版本、安裝檔名、Windows 版本、官方工具版本
  與完整錯誤。截圖前遮住帳號資訊；不要傳送密碼、token 或登入資料。

## 資料與隱私

AI Usage 的帳號卡片、顯示偏好與上次用量主要儲存在：

```text
%LOCALAPPDATA%\AiUsageDashboard
```

AI Usage 透過各服務的官方工具或 SDK 完成登入與用量查詢，
不會讀取密碼或瀏覽器登入資料。

Copilot 每張卡片的 token 會分開保存在 Windows 認證管理員，
不會寫入帳號設定、上次用量、匯出檔或診斷紀錄。
Grok 也會依卡片分開保存本機登入資料。

使用 Antigravity 時，AI Usage 會保存這台電腦所需的連接設定。
卡片顯示的電子郵件與方案只存於本機，不會寫入匯出檔，也不會當成登入憑證。

安裝包已包含 Copilot 所需元件，並附上
[Copilot CLI](third-party-notices/GitHub-Copilot-CLI-LICENSE.md)與
[Copilot SDK](third-party-notices/GitHub-Copilot-SDK-LICENSE.md)的第三方授權聲明。

## 進階資料

### 核對 ZIP 的 SHA-256

ZIP 經過聊天軟體、網路磁碟或他人轉傳時，請從另一個可信任管道取得同版本的 SHA-256，
例如已核對的正式 Release 發布資訊。不要只使用和 ZIP 一起轉傳的雜湊值。

在 ZIP 所在的資料夾開啟 PowerShell，執行下列命令，並把檔名換成實際版本：

```powershell
Get-FileHash -LiteralPath ".\AiUsageDashboard-<version>-win-x64.zip" -Algorithm SHA256
```

畫面中的 `Hash` 必須與可信任管道提供的值相同。同一個下載位置提供的 ZIP 與
`.sha256` 可以檢查下載是否損壞，但不能單獨證明檔案由誰發布。

### 維護文件

一般使用者不需要了解內部資料格式或服務串接方式。以下維護文件以目前 `main` 原始碼為準，可能和已安裝版本不同。具備專案權限的維護人員可查閱：

- [技術總覽][technical-overview]：程式架構、資料保存與服務串接。
- [Windows 分發與支援手冊][internal-distribution]：安裝、更新、回復與清理。
- [發佈流程][releasing]：建立與驗證候選版。

[project-readme]: https://github.com/Sokaka/ai-usage-dashboard/blob/main/README.md#目前版本
[technical-overview]: https://github.com/Sokaka/ai-usage-dashboard/blob/main/docs/TECHNICAL_OVERVIEW.md
[internal-distribution]: https://github.com/Sokaka/ai-usage-dashboard/blob/main/INTERNAL_DISTRIBUTION.md
[releasing]: https://github.com/Sokaka/ai-usage-dashboard/blob/main/RELEASING.md
